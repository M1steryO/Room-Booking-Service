$kind: http-request
url: "{{baseUrl}}/rooms/list"
method: GET
headers:
  Accept: application/json
body:
  type: text
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Список переговорок (admin и user).resources/examples
order: 1000
