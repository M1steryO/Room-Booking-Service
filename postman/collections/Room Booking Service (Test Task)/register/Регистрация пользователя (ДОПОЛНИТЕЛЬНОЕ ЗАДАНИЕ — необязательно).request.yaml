$kind: http-request
description: >
  Создаёт нового пользователя и возвращает его данные. Реализация этого
  эндпоинта является **дополнительным заданием**. Для авторизации в рамках
  обязательной части используйте `/dummyLogin`. Доступен без авторизации.
url: "{{baseUrl}}/register"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "email": "3MSHMD6BYY-HO9@uNDrdscHntbtPMpkofmlE.scj",
      "password": "string",
      "role": "admin"
    }
examples: ./.resources/Регистрация пользователя (ДОПОЛНИТЕЛЬНОЕ ЗАДАНИЕ —
  необязательно).resources/examples
order: 1000
